const quarterOf = (month) => {
  // we have three quarters
  // each month represents an interger
  // each quarter is split up by 4
  // start off with an if statement is month is <= to the quarter of months then we will r
  // return
  
  if( month <= 3){
    return 1
  }else if( month <= 6 ){
    return 2
  }else if( month <= 9){
    return 3
  }else if( month <= 12){
    return 4
  }
​
   
  
}