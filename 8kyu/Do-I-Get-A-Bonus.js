function bonusTime(salary, bonus) {
    // function takes in two arguements
    //salary interger
    //bonus is a bool
      
      if (bonus === true) {
        return `£${salary * 10}`;
      } else {
        return `£${salary}`;
      }
}
    
      